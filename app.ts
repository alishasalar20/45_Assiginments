let members: string[] =[`alisha`,`rohan`,`eshah`,`laiba`];
let message: string=`would you like to spend this ramadan with us`;
for (let i=0; i<members.length; i++){
    console.log(message + members[i]);
}